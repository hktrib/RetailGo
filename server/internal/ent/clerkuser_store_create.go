// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/hktrib/RetailGo/internal/ent/clerkuser_store"
)

// ClerkUserStoreCreate is the builder for creating a ClerkUser_Store entity.
type ClerkUserStoreCreate struct {
	config
	mutation *ClerkUserStoreMutation
	hooks    []Hook
}

// SetClerkID sets the "clerk_id" field.
func (cusc *ClerkUserStoreCreate) SetClerkID(s string) *ClerkUserStoreCreate {
	cusc.mutation.SetClerkID(s)
	return cusc
}

// SetStoreID sets the "store_id" field.
func (cusc *ClerkUserStoreCreate) SetStoreID(i int) *ClerkUserStoreCreate {
	cusc.mutation.SetStoreID(i)
	return cusc
}

// Mutation returns the ClerkUserStoreMutation object of the builder.
func (cusc *ClerkUserStoreCreate) Mutation() *ClerkUserStoreMutation {
	return cusc.mutation
}

// Save creates the ClerkUser_Store in the database.
func (cusc *ClerkUserStoreCreate) Save(ctx context.Context) (*ClerkUser_Store, error) {
	return withHooks(ctx, cusc.sqlSave, cusc.mutation, cusc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (cusc *ClerkUserStoreCreate) SaveX(ctx context.Context) *ClerkUser_Store {
	v, err := cusc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (cusc *ClerkUserStoreCreate) Exec(ctx context.Context) error {
	_, err := cusc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (cusc *ClerkUserStoreCreate) ExecX(ctx context.Context) {
	if err := cusc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (cusc *ClerkUserStoreCreate) check() error {
	if _, ok := cusc.mutation.ClerkID(); !ok {
		return &ValidationError{Name: "clerk_id", err: errors.New(`ent: missing required field "ClerkUser_Store.clerk_id"`)}
	}
	if _, ok := cusc.mutation.StoreID(); !ok {
		return &ValidationError{Name: "store_id", err: errors.New(`ent: missing required field "ClerkUser_Store.store_id"`)}
	}
	return nil
}

func (cusc *ClerkUserStoreCreate) sqlSave(ctx context.Context) (*ClerkUser_Store, error) {
	if err := cusc.check(); err != nil {
		return nil, err
	}
	_node, _spec := cusc.createSpec()
	if err := sqlgraph.CreateNode(ctx, cusc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	cusc.mutation.id = &_node.ID
	cusc.mutation.done = true
	return _node, nil
}

func (cusc *ClerkUserStoreCreate) createSpec() (*ClerkUser_Store, *sqlgraph.CreateSpec) {
	var (
		_node = &ClerkUser_Store{config: cusc.config}
		_spec = sqlgraph.NewCreateSpec(clerkuser_store.Table, sqlgraph.NewFieldSpec(clerkuser_store.FieldID, field.TypeInt))
	)
	if value, ok := cusc.mutation.ClerkID(); ok {
		_spec.SetField(clerkuser_store.FieldClerkID, field.TypeString, value)
		_node.ClerkID = value
	}
	if value, ok := cusc.mutation.StoreID(); ok {
		_spec.SetField(clerkuser_store.FieldStoreID, field.TypeInt, value)
		_node.StoreID = value
	}
	return _node, _spec
}

// ClerkUserStoreCreateBulk is the builder for creating many ClerkUser_Store entities in bulk.
type ClerkUserStoreCreateBulk struct {
	config
	err      error
	builders []*ClerkUserStoreCreate
}

// Save creates the ClerkUser_Store entities in the database.
func (cuscb *ClerkUserStoreCreateBulk) Save(ctx context.Context) ([]*ClerkUser_Store, error) {
	if cuscb.err != nil {
		return nil, cuscb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(cuscb.builders))
	nodes := make([]*ClerkUser_Store, len(cuscb.builders))
	mutators := make([]Mutator, len(cuscb.builders))
	for i := range cuscb.builders {
		func(i int, root context.Context) {
			builder := cuscb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ClerkUserStoreMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, cuscb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, cuscb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, cuscb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (cuscb *ClerkUserStoreCreateBulk) SaveX(ctx context.Context) []*ClerkUser_Store {
	v, err := cuscb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (cuscb *ClerkUserStoreCreateBulk) Exec(ctx context.Context) error {
	_, err := cuscb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (cuscb *ClerkUserStoreCreateBulk) ExecX(ctx context.Context) {
	if err := cuscb.Exec(ctx); err != nil {
		panic(err)
	}
}
